#include<bits/stdc++.h>
using namespace std;

using ll = long long ;
using pii = pair<int , int>;
#define ss second
#define ff first
#define RAFTAR ios::sync_with_stdio(false); cin.tie(0); cout.tie(0);
#define sz(x) (int)(x).size()
#define all(x) begin(x), end(x)
#define pb push_back
#define FOR(i,a,b) for (int i = (a); i < (b); ++i)
#define ROF(i,a,b) for (int i = (b)-1; i >= (a); --i)


void SOLVER();

int main(){
    
    RAFTAR;

    int numberOfTestCase;
    cin >> numberOfTestCase;
    
    FOR(i,0,numberOfTestCase){
        SOLVER();
    }

    return 0;
}
      
void SOLVER(){
    int n; cin >> n;
}
